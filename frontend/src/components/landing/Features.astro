---
import Icon from '../ui/Icon.astro';
---

<section class="features-section">
    <div class="container">
        <div class="section-header">
            <div class="eyebrow-box">
                <span class="eyebrow">A Plataforma</span>
            </div>
            <h2>Ferramentas de <span class="text-gradient">Elite</span></h2>
            <p>Tudo o que você precisa para sair do tutorial hell e evoluir em seus estudos.</p>
        </div>

        <div class="bento-grid">
            <!-- Large Item: Playground -->
            <div class="grid-item item-playground">
                <div class="card-bg"></div>
                <div class="card-content">
                    <div class="card-header">
                        <div class="icon-box">
                            <Icon name="code" size={24} />
                        </div>
                        <h3>Playground Nativo</h3>
                    </div>
                    <p>Compile e execute C diretamente no navegador via WebAssembly. Sem configuração.</p>
                    
                    <div class="mini-terminal">
                        <div class="terminal-header">
                            <span class="dot red"></span>
                            <span class="dot yellow"></span>
                            <span class="dot green"></span>
                        </div>
                        <div class="terminal-body pseudo-code">
                            <div><span class="k">int</span> <span class="f">main</span>() {'{'}</div>
                            <div class="indent">  <span class="t">printf</span>(<span class="s">"Hello World\n"</span>);</div>
                            <div class="indent">  <span class="k">return</span> <span class="n">0</span>;</div>
                            <div>{'}'}</div>
                            <div class="mt-2"><span class="cmd">$</span> gcc main.c -o app</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Medium Item: Podcast -->
            <div class="grid-item item-podcast">
                <div class="card-bg"></div>
                <div class="card-content">
                    <div class="card-header">
                        <div class="icon-box purple">
                            <Icon name="mic" size={24} />
                        </div>
                        <h3>Podcast AI</h3>
                    </div>
                    <p>Esculte em qualquer lugar com áudio gerado neuralmente.</p>
                    <div class="waveform-viz">
                        <span class="bar"></span><span class="bar"></span><span class="bar"></span><span class="bar"></span>
                    </div>
                </div>
            </div>

            <!-- Small Item: Hardware -->
            <div class="grid-item item-small">
                <div class="card-header">
                    <div class="icon-box blue">
                        <Icon name="cpu" size={24} />
                    </div>
                    <h3>Hardware</h3>
                </div>
                <p>Aprenda sobre Drivers e ESP32.</p>
            </div>

            <!-- Small Item: Architecture -->
            <div class="grid-item item-small">
                <div class="card-header">
                    <div class="icon-box green">
                        <Icon name="layers" size={24} />
                    </div>
                    <h3>Arquitetura</h3>
                </div>
                <p>Pratique e estude sobre Cache & Pipeline.</p>
            </div>

        </div>
    </div>
</section>

<style>
    .features-section {
        padding: 120px 24px;
        background: #050505;
        position: relative;
        overflow: hidden;
    }

    /* Ambient Glow behind grid */
    .features-section::before {
        content: '';
        position: absolute;
        width: 600px;
        height: 600px;
        background: radial-gradient(circle, rgba(59,130,246,0.08) 0%, transparent 70%);
        top: 20%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 0;
        pointer-events: none;
    }

    .container {
        max-width: 1100px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
    }

    .section-header {
        text-align: center;
        margin-bottom: 80px;
    }

    .eyebrow-box {
        display: inline-block;
        margin-bottom: 16px;
    }

    .eyebrow {
        color: #a1a1aa;
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        border: 1px solid rgba(255,255,255,0.1);
        padding: 6px 12px;
        border-radius: 99px;
        display: inline-block;
    }

    h2 {
        font-size: 3rem;
        font-weight: 800;
        margin-bottom: 24px;
        color: #fff;
        letter-spacing: -0.02em;
    }

    .text-gradient {
        background: linear-gradient(to right, #60a5fa, #a78bfa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .section-header p {
        color: #a1a1aa;
        font-size: 1.125rem;
        max-width: 500px;
        margin: 0 auto;
        line-height: 1.6;
    }

    /* Bento Grid Layout */
    .bento-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        grid-auto-rows: minmax(180px, auto);
        gap: 20px;
    }

    /* Glass Cards */
    .grid-item {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 20px;
        padding: 24px;
        position: relative;
        overflow: hidden;
        transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .card-bg {
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at top right, rgba(255,255,255,0.05), transparent 60%);
        opacity: 0;
        transition: opacity 0.4s ease;
    }

    .grid-item:hover {
        transform: translateY(-4px);
        border-color: rgba(255, 255, 255, 0.2);
        box-shadow: 0 20px 40px -10px rgba(0,0,0,0.5);
    }

    .grid-item:hover .card-bg {
        opacity: 1;
    }

    /* Specific Items */
    .item-playground {
        grid-column: span 2;
        grid-row: span 2;
    }

    .item-podcast {
        grid-column: span 2;
    }

    .item-small {
        grid-column: span 1;
    }

    /* Content Styling */
    .card-header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 12px;
    }

    .icon-box {
        width: 40px;
        height: 40px;
        border-radius: 10px;
        background: rgba(255,255,255,0.05);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #d4d4d8;
    }
    .icon-box.purple { color: #a78bfa; background: rgba(167, 139, 250, 0.1); }
    .icon-box.blue { color: #60a5fa; background: rgba(96, 165, 250, 0.1); }
    .icon-box.green { color: #4ade80; background: rgba(74, 222, 128, 0.1); }

    h3 {
        color: #fff;
        font-size: 1.1rem;
        font-weight: 600;
        margin: 0;
    }

    p {
        color: #a1a1aa;
        font-size: 0.9rem;
        line-height: 1.5;
        margin: 0;
    }

    /* Terminal Viz */
    .mini-terminal {
        margin-top: 24px;
        background: #0f0f11;
        border-radius: 8px;
        border: 1px solid #27272a;
        padding: 12px;
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.75rem;
    }
    
    .terminal-header { display: flex; gap: 6px; margin-bottom: 12px; }
    .dot { width: 8px; height: 8px; border-radius: 50%; }
    .red { background: #ef4444; } .yellow { background: #eab308; } .green { background: #22c55e; }

    .pseudo-code { color: #a1a1aa; line-height: 1.4; }
    .k { color: #c084fc; } .f { color: #60a5fa; } .s { color: #86efac; } .n { color: #fbbf24; }
    .indent { padding-left: 12px; }
    .cmd { color: #4ade80; margin-right: 6px; }
    .t { color: #dcdcaa; }

    /* Waveform Viz */
    .waveform-viz {
        display: flex;
        gap: 4px;
        align-items: center;
        height: 24px;
        margin-top: 16px;
    }
    .bar {
        width: 4px;
        background: #a78bfa;
        border-radius: 2px;
        animation: dance 1s ease-in-out infinite;
    }
    .bar:nth-child(1) { height: 12px; animation-delay: 0.1s; }
    .bar:nth-child(2) { height: 20px; animation-delay: 0.2s; }
    .bar:nth-child(3) { height: 16px; animation-delay: 0.3s; }
    .bar:nth-child(4) { height: 10px; animation-delay: 0.4s; }

    @keyframes dance {
        0%, 100% { transform: scaleY(1); }
        50% { transform: scaleY(0.5); }
    }

    @media (max-width: 1024px) {
        .bento-grid { grid-template-columns: repeat(2, 1fr); }
        .item-playground { grid-column: span 2; }
    }

    @media (max-width: 640px) {
        .bento-grid { grid-template-columns: 1fr; }
        .item-playground, .item-podcast, .item-small { grid-column: span 1; }
    }
</style>
